<div class='list-group'>
{{#each twoots}}
	<blockquote class="pull-left list-group-item col-xs-12" name='{{this.author.name}}'>
	    <p>{{this.text}}</p>
	    <small>{{this.author.name}}</small>
	</blockquote>
{{/each}}
</div>